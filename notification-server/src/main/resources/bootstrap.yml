spring:
  application:
    name: trak-notification-server
  cloud:
    config:
      discovery:
        enabled: true
        service-id: trak-config-server
      enabled: true
  profiles:
    active: development