eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka

spring:
  application:
    name: trak-email-server
  cloud:
    config:
      discovery:
        enabled: true
        service-id: trak-config-server
      enabled: true
